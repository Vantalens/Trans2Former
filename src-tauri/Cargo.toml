[package]
name = "trans2former"
version = "2.3.0"
description = "Trans2Former Desktop"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
# 修复 issue #30: 移除未使用的 fs/dialog 插件（前端走浏览器 File API）
tauri = { version = "2", features = [] }
