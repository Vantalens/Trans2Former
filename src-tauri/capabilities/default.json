{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main Trans2Former window - minimal permissions (no fs/dialog APIs in use).",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
